from setuptools import find_packages, setup

setup(
    name='scrapper',
    version='0.0.1',
    author='Aagam',
    author_email='velakoc316@jofuso.com',
    packages=find_packages(),
    install_requires=[]
)